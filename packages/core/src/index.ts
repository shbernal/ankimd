/**
 * The public surface of `@ankimd/core`. Empty until the spec scanner lands.
 */

/*
 * `isolatedModules` wants a module marker and there is nothing to export yet.
 * The directive goes away with the first real export.
 */
// oxlint-disable-next-line unicorn/require-module-specifiers
export {};
